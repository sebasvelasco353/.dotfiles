- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.p10k.zsh:
    ~/.zshrc:
    ~/init.vim: nvim/init.vim

- create:
    - ~/.config/nvim/colors
    - ~/.config/nvim/plugged
    - ~/.config/nvim/undodir

- shell:
  - 
    command: ./setup_utilities.zsh
    stdout: true
    stderr: true

