- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.p10k.zsh:
    ~/.zshrc:

# - create:
    # - ~/holis

- shell:
  - 
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true