- defaults:
    link:
      relink: true

- clean: ['~']

#- link:
#    ~/.gitconfig: .gitconfig

- create:
    - ~/holis

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]